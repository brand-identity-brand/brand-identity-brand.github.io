# Personal Site

This directory will contain the deployable website. Its entry point will mount
the configured personal OS from the workspace's `packages/os` package.

Site-owned concerns include HTML, public assets, metadata, analytics,
top-level error handling, and static-host deployment.

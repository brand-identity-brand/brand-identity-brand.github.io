# Independent Projects

This directory will contain independently maintained repositories as Git
submodules. The first planned project is `react-desktop-environment`, which is
the reusable OS/runtime consumed by the top-level `os` configuration package.

Submodules are intentionally not scaffolded as ordinary directories. Each one
will be added when its repository URL and pinned starting commit are selected.
